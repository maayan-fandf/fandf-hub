node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vercel
next-env.d.ts
*.tsbuildinfo
# Claude Code local config (may contain Bash-permission allowlists with
# inline API tokens — must NEVER be committed).
.claude/
# Temp VAPID private-key file from the web-push key-generation step.
# The private key belongs in Secret Manager, NEVER in the repo.
.vapid_private.txt
vapid_private.txt
# Throwaway data-probe scripts (read .env.local locally; never part of a
# deploy). Kept out so `git add -A` on a version commit doesn't sweep them in.
scripts/probe-*.mjs
scripts/probe-*.ts
